// FILE: shared/constants/pagination.js

/**
 * Default page number for pagination
 * @type {number}
 */
const DEFAULT_PAGE = 1;

/**
 * Default number of items per page
 * @type {number}
 */
const DEFAULT_LIMIT = 20;

/**
 * Maximum allowed items per page
 * @type {number}
 */
const MAX_LIMIT = 100;

module.exports = {
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT
};
